// prettier caches the config -- uncomment the line below and run `node .pretterrc.js` to force a reload
// require("prettier").clearConfigCache();
module.exports = {
        semi: true,
        trailingComma: "all",
        printWidth: 120,
        tabWidth: 4,
};
